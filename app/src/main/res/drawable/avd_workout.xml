<animated-vector xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:aapt="http://schemas.android.com/aapt">

    <aapt:attr name="android:drawable">
        <vector
            android:width="24dp"
            android:height="24dp"
            android:viewportWidth="24"
            android:viewportHeight="24">

            <!-- Camera Group (Rotating) -->
            <group
                android:name="rotationGroup"
                android:pivotX="12"
                android:pivotY="12"
                android:rotation="0">

                <!-- Camera Body -->
                <path
                    android:name="cameraBody"
                    android:fillColor="#4CAF50"
                    android:pathData="M4 8L6 6H10L12 4H14L16 6H20L22 8V18C22 19.1 21.1 20 20 20H4C2.9 20 2 19.1 2 18V8Z" />

                <!-- Camera Lens Animation using Path Morphing -->
                <path
                    android:name="cameraLens"
                    android:fillColor="#FFFFFF"
                    android:pathData="M 10,12 A 2,2 0 1,1 14,12 A 2,2 0 1,1 10,12 Z" />
            </group>

        </vector>
    </aapt:attr>

    <!-- Rotate the Camera -->
    <target android:name="rotationGroup">
        <aapt:attr name="android:animation">
            <objectAnimator
                android:propertyName="rotation"
                android:duration="500"
                android:valueFrom="0"
                android:valueTo="360"
                android:repeatMode="reverse"
                android:repeatCount="1" />
        </aapt:attr>
    </target>

    <!-- Animate Lens Expansion using Path Morphing -->
    <target android:name="cameraLens">
        <aapt:attr name="android:animation">
            <set>
                <objectAnimator
                    android:propertyName="pathData"
                    android:duration="300"
                    android:valueFrom="M 10,12 A 2,2 0 1,1 14,12 A 2,2 0 1,1 10,12 Z"
                    android:valueTo="M 9,12 A 3,3 0 1,1 15,12 A 3,3 0 1,1 9,12 Z"
                    android:valueType="pathType"
                    android:repeatMode="reverse"
                    android:repeatCount="1" />
            </set>
        </aapt:attr>
    </target>

</animated-vector>
